# --- build output ---
dnac
*.exe
feature_test
*.o
*.obj
*.pdb
*.ilk

# --- genome data (downloaded via scripts/get-data.sh, never committed) ---
*.fa
*.fna
*.fasta
*.seq
*.fa.gz
*.fna.gz
*.acgt

# --- codec output / test artifacts ---
*.dnac
# ...except the stored v0.8.0 streams: v0.9.0 must decode them byte for byte,
# and scripts/roundtrip.sh reads them from a clean checkout (tests/v080/make.sh)
!tests/v080/*.dnac
*.state
*.zip
*.zst
*.xz
/roundtrip.*
/sample.*
results/

# --- third-party code fetched for benchmarking (own licences) ---
bench-external/
sources/

# --- local working notes / agent configuration ---
.claude/
CLAUDE.md
AGENTS_SETUP.md
blog-dnac-journey.md
dnac_dnc9_backup.c

# --- byte-level side branch (separate experiment, not part of this codec) ---
text-offshoot/

# --- python bytecode (scripts/cue) ---
__pycache__/
*.pyc

# --- OS ---
Thumbs.db
.DS_Store
